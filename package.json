{
  "name": "drizby-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && vite build --config vite.config.worker.ts",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy --env production",
    "deploy:staging": "npm run build && wrangler deploy --env staging",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "0.35.2",
    "astro": "5.13.4",
    "sharp": "^0.34.2"
  },
  "overrides": {
    "zod": "3.25.76",
    "@astrojs/sitemap": "3.5.1"
  },
  "devDependencies": {
    "pagefind": "^1.4.0",
    "vite": "^5.4.1",
    "wrangler": "^4.32.0"
  }
}
